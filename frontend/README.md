# AI Security Recommendation Frontend

Simple React + Vite frontend for an AI Security Recommendation System.

## Setup

```bash
npm install
npm run dev
```

Create a `.env` file from `.env.example` and set your backend URL:

```bash
VITE_API_URL=http://localhost:5000
```
